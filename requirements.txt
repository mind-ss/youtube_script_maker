# 네이버 블로그 작성 도구
# Python 3.8 이상 필요

# 핵심 의존성 (표준 라이브러리 사용으로 별도 설치 불필요)
# - dataclasses (Python 3.7+)
# - typing (Python 3.5+)
# - re, argparse, pathlib 등

# 선택적 의존성 (추가 기능용)

# 마크다운 렌더링 및 프리뷰
markdown>=3.4.0

# 한글 텍스트 처리
konlpy>=0.6.0  # 선택사항: 키워드 추출 등에 활용 가능

# 코드 품질
black>=23.0.0  # 코드 포맷팅
flake8>=6.0.0  # 린팅
mypy>=1.0.0    # 타입 체킹

# 테스트
pytest>=7.0.0
pytest-cov>=4.0.0
